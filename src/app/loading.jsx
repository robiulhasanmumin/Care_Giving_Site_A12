 
const Loading = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen w-full">
      {/* DaisyUI Loading Spinner */}
      <span className="text-5xl font-bold animate-pulse text-primary">Loading...</span>
     </div>
  );
};

export default Loading;